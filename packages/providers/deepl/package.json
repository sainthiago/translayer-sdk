{
  "name": "@translayer/providers-deepl",
  "version": "0.1.0",
  "type": "module",
  "private": false,
  "sideEffects": false,
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "dependencies": {
    "@translayer/core": "workspace:*"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint not configured yet)\"",
    "clean": "rm -rf dist"
  }
}

